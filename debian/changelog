cpansec-feed (0.2.0) unstable; urgency=medium

  * Redesign validation report: one row per vulnerability, sorted newest first.
  * Add Published and Updated date columns with relative time display.
  * Make Distribution and date columns sortable.
  * Add clickable links for CVE records (cve.org) and CPANSA files (GitHub).
  * Stop merging CPANSA data into CPANSec authoritative CVE records.
  * Deduplicate CPANSA source advisories and report rows.
  * Distinguish CPANSec CVE, CPANSA (external CNA), and CPANSA-only statuses.
  * Rename report title to CPANSec Feed.

 -- CPANSec Feed Maintainers <packages@example.invalid>  Fri, 04 Apr 2026 21:30:00 +0000

cpansec-feed (0.1.11) unstable; urgency=medium

  * Stop setting setgid on per-release publish directories.
  * Use a service umask that keeps published files traversable by nginx.
  * Improve report layout for long metadata and skipped-record lists.
  * Create /var/www/cpansec-feed/.releases via tmpfiles for stable ownership.
  * Prune old published site releases after successful swaps.
  * Fix published release directory permissions for nginx access.
  * Publish the site atomically through a current/ symlink.
  * Keep current files untouched when generated content is unchanged.
  * Add source freshness metadata to the HTML pages.
  * Include exact skip reasons in the validation report.
  * Allow the service to write published output under /var/www/cpansec-feed.
  * Publish generated assets under /var/www/cpansec-feed.
  * Make published output files world-readable for nginx.
  * Create the public output directory for the www-data group.
  * Fix /etc/default/cpansec-feed packaging path.
  * Drop tmpfiles entries that conflicted with DynamicUser.
  * Add make targets for the podman-based workflow.

 -- CPANSec Feed Maintainers <packages@example.invalid>  Tue, 17 Mar 2026 03:40:00 +0100
